
services:
  app:
    build: .
    ports:
      - "3001:3000"
    environment:
      - DB_URI=postgresql://postgres:postgres@db:5432/goit_db
      - DB_SSL=false
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: goit_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d goit_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
